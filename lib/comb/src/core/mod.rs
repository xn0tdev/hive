//! Core value types — the data model everything else draws on: geometry, colour
//! and text style, styled text runs, and the cell buffer that backs the screen.

pub mod buffer;
pub mod geom;
pub mod style;
pub mod text;
